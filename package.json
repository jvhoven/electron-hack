{
  "name": "Hack",
  "version": "0.1.0",
  "main": "app.js",
  "dependencies": {
    "jquery": "^2.1.4",
    "lowdb": "^0.7.3",
    "page": "^1.6.3",
    "ractive": "^0.7.3"
  },
  "scripts": {
    "postinstall": "cd node_modules && mkdir __base && cd __base && echo module.exports = require('path').resolve(__dirname + '/../../') + '/'; > index.js"
  },
  "devDependencies": {
    "electron-prebuilt": "^0.25.2"
  }
}
